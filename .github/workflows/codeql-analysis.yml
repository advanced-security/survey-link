name: "Test"

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  analyze:
    name: Test
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write

    steps:
    - name: Create Link
      id: createlink
      uses: zbazztian/surveymonkey-app@main
      with:
        surveymonkey_token: ${{ secrets.surveymonkey_token }}
        name: acmecorp

    - run: |
       echo "${{ steps.createlink.outputs.link }}"
