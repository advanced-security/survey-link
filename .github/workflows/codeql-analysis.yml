name: "Test Action"

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  analyze:
    name: Test
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write

    steps:
    - name: Create Link
      id: createlink
      uses: zbazztian/survey-link@main
      with:
        surveymonkey_token: ${{ secrets.surveymonkey_token }}
        name: acmecorp deluxe 423 3453 ASfa;l __;ajsdflajjlj; ASfa;l __;ajsdflajjlj;.@bla

    - run: |
       echo "${{ steps.createlink.outputs.link }}"
